name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl backend
        # Replace with your actual backend URL if different
        run: curl -s https://f1pedia-backend.onrender.com/health
